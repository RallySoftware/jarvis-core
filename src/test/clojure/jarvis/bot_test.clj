(ns jarvis.bot-test
  (:use clojure.test)
  (:require [jarvis.bot :as bot]
            [jarvis.test-util :as test-util]))

;would mainly consist of tests for clj-flow et al. to be deleted?
; we could test the when-let conditions?  i don't really see how else to test this right now.